#include <stdio.h>
#include <string.h>

int main() {
    char str[50];
    printf("Enter string: ");
    scanf("%s", str);

    int n = strlen(str);

    if(str[0] == '0' && str[n-1] == '1')
        printf("Accepted");
    else
        printf("Rejected");

    return 0;
}
