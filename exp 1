#include <stdio.h>
#include <string.h>

int main() {
    char str[50];
    printf("Enter string: ");
    scanf("%s", str);

    int n = strlen(str);

    if(str[0] == 'a' && str[n-1] == 'a')
        printf("Accepted");
    else
        printf("Rejected");

    return 0;
}
